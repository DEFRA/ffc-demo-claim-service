version: '3'

# This override file should be used when running this service in isolation
# to provide required sibling services for local testing

services:
  ffc-demo-claim-service:
    command: >
      sh -c "
        scripts/wait-for/wait-for ffc-demo-claim-postgres:5432 --
        npm run migrate &&
        scripts/wait-for/wait-for ffc-demo-claim-message-queue:8161 --
        npm run start:watch"
    depends_on:
      - ffc-demo-claim-message-queue
    networks:
      - ffc-demo-claim

  ffc-demo-claim-message-queue:
    container_name: ffc-demo-claim-message-queue
    image: vromero/activemq-artemis:2.9.0-alpine
    environment:
      ARTEMIS_USERNAME: artemis
      ARTEMIS_PASSWORD: artemis
    networks:
      - ffc-demo-claim
    ports:
      - "8161:8161"
      - "5672:5672"
    volumes:
      - message-queue-data:/var/lib/artemis/data
      - message-queue-tmp:/var/lib/artemis/tmp
      - message-queue-etc:/var/lib/artemis/etc
      - message-queue-etc-override:/var/lib/artemis/etc-override
      - message-queue-lock:/var/lib/artemis/lock

networks:
  ffc-demo-claim: {}

volumes:
  message-queue-data: {}
  message-queue-tmp: {}
  message-queue-etc: {}
  message-queue-etc-override: {}
  message-queue-lock: {}
