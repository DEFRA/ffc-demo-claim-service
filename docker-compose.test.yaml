version: '3.4'
services:
  mine-support-claim-service:
    volumes:
    - node_modules:/usr/src/app/node_modules
    - .:/usr/src/app
    command: /bin/sh -c ./wait-for mine-support-postgres-claims:5432 -- /bin/sh ./migrate-test

volumes:
  node_modules:
