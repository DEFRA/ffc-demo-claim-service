{{- include "adp-helm-library.deployment" (list . "adp-demo-claim-service.deployment") -}}
{{- define "adp-demo-claim-service.deployment" -}}
spec:
  template:
    spec:
      containers:
      - {{ include "adp-helm-library.container" (list . "adp-demo-claim-service.container") }}
{{- end -}}
